# Database driver: sqlite | postgres
DATABASE_DRIVER=sqlite

# Postgres connection (only when DATABASE_DRIVER=postgres)
DATABASE_URL=postgres://user:password@host:5432/readtree

# WeRead cookie (optional, can be set in app settings)
WEREAD_COOKIE=
